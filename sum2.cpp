#include <iostream>

using namespace std;

int main() {
    int n;
    int sum = 0;

    cout << "Enter 5 numbers:" << endl;

    cin >> n;
    sum = sum + n;

    cin >> n;
    sum = sum + n;

    cin >> n;
    sum = sum + n;

    cin >> n;
    sum = sum + n;

    cin >> n;
    sum = sum + n;

    cout << "Total sum: " << sum << endl;

    return 0;
}